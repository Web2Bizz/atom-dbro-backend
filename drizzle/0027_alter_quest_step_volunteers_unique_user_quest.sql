-- Миграция больше не меняет схему.
-- Уникальность по (quest_id, type, user_id) настраивается в 0026_alter_quest_step_volunteers.sql.
-- Оставляем файл, чтобы не ломать порядок миграций, но фактически это no-op.

DO $$
BEGIN
  -- Ничего не делаем
END;
$$;

